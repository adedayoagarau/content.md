# Challenge 044: Saved video no longer available

This original synthetic English-only scenario covers the `media_subscriptions` domain, a `content_removed` state, and a `empty` surface at `low` risk.

It is frozen at the authored-scenario stage. No content.md candidate, Claude review, Cursor review, human adjudication, benchmark label, or effectiveness claim exists yet.
