# Challenge 040: Workspace export ready

This original synthetic English-only scenario covers the `work_collaboration` domain, a `success_with_expiry` state, and a `email` surface at `medium` risk.

It is frozen at the authored-scenario stage. No content.md candidate, Claude review, Cursor review, human adjudication, benchmark label, or effectiveness claim exists yet.
