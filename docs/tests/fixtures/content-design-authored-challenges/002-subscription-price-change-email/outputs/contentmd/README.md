# content.md output

`candidate.json` is the exact unedited repository-agent candidate. Generator
metadata distinguishes `content.md` from its Codex host. `review-request.json`,
`review.json`, and `repair-brief.json` preserve the shipped deterministic
diagnosis of the starting email.

These artifacts are proposals and evaluation inputs. They are not approved
subscription content or evidence of effectiveness.
