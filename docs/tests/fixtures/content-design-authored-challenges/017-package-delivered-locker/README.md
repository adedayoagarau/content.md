# Challenge 017: Package delivered to a locker

This original synthetic English-only scenario covers the `commerce_delivery` domain, a `success_with_instructions` state, and a `sms` surface at `medium` risk.

It is frozen at the authored-scenario stage. No content.md candidate, Claude review, Cursor review, human adjudication, benchmark label, or effectiveness claim exists yet.
