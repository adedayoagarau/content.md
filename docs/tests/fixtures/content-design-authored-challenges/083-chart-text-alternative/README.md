# Challenge 083: Chart needs a text alternative

This original synthetic English-only scenario covers the `accessibility_assistive` domain, a `accessibility_guidance` state, and a `form` surface at `medium` risk.

It is frozen at the authored-scenario stage. No content.md candidate, Claude review, Cursor review, human adjudication, benchmark label, or effectiveness claim exists yet.
