# Challenge 021: Train platform changed

This original synthetic English-only scenario covers the `travel_mobility` domain, a `service_change` state, and a `push` surface at `high` risk.

It is frozen at the authored-scenario stage. No content.md candidate, Claude review, Cursor review, human adjudication, benchmark label, or effectiveness claim exists yet.
