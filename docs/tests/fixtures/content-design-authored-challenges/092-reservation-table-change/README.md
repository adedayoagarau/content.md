# Challenge 092: Restaurant reservation table changed

This original synthetic English-only scenario covers the `events_bookings` domain, a `material_change` state, and a `sms` surface at `medium` risk.

It is frozen at the authored-scenario stage. No content.md candidate, Claude review, Cursor review, human adjudication, benchmark label, or effectiveness claim exists yet.
