# Challenge 094: Museum entry time sold out

This original synthetic English-only scenario covers the `events_bookings` domain, a `availability_error` state, and a `inline` surface at `low` risk.

It is frozen at the authored-scenario stage. No content.md candidate, Claude review, Cursor review, human adjudication, benchmark label, or effectiveness claim exists yet.
