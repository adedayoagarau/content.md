# Challenge 065: Home claim inspection rescheduled

This original synthetic English-only scenario covers the `insurance_claims` domain, a `schedule_change` state, and a `sms` surface at `high` risk.

It is frozen at the authored-scenario stage. No content.md candidate, Claude review, Cursor review, human adjudication, benchmark label, or effectiveness claim exists yet.
