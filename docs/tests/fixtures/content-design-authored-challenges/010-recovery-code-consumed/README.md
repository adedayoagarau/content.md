# Challenge 010: Recovery code used successfully

This original synthetic English-only scenario covers the `account_security` domain, a `success_with_follow_up` state, and a `confirmation` surface at `high` risk.

It is frozen at the authored-scenario stage. No content.md candidate, Claude review, Cursor review, human adjudication, benchmark label, or effectiveness claim exists yet.
