# Challenge 012: Bank transfer still pending

This original synthetic English-only scenario covers the `payments_billing` domain, a `pending` state, and a `status` surface at `high` risk.

It is frozen at the authored-scenario stage. No content.md candidate, Claude review, Cursor review, human adjudication, benchmark label, or effectiveness claim exists yet.
