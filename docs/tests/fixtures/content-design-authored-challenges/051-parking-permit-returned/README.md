# Challenge 051: Parking permit application returned

This original synthetic English-only scenario covers the `government_civic` domain, a `application_returned` state, and a `email` surface at `medium` risk.

It is frozen at the authored-scenario stage. No content.md candidate, Claude review, Cursor review, human adjudication, benchmark label, or effectiveness claim exists yet.
