# Challenge 007: Suspicious sign-in confirmation

This original synthetic English-only scenario covers the `account_security` domain, a `verification_required` state, and a `email` surface at `critical` risk.

It is frozen at the authored-scenario stage. No content.md candidate, Claude review, Cursor review, human adjudication, benchmark label, or effectiveness claim exists yet.
