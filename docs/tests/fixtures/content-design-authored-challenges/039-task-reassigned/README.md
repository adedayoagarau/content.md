# Challenge 039: Task reassigned with unfinished work

This original synthetic English-only scenario covers the `work_collaboration` domain, a `ownership_change` state, and a `push` surface at `medium` risk.

It is frozen at the authored-scenario stage. No content.md candidate, Claude review, Cursor review, human adjudication, benchmark label, or effectiveness claim exists yet.
