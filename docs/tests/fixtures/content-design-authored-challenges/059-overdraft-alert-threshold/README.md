# Challenge 059: Overdraft alert threshold changed

This original synthetic English-only scenario covers the `banking_savings` domain, a `preference_change` state, and a `confirmation` surface at `medium` risk.

It is frozen at the authored-scenario stage. No content.md candidate, Claude review, Cursor review, human adjudication, benchmark label, or effectiveness claim exists yet.
