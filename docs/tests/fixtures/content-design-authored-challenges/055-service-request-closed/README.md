# Challenge 055: City service request closed without a fix

This original synthetic English-only scenario covers the `government_civic` domain, a `closure_without_resolution` state, and a `status` surface at `high` risk.

It is frozen at the authored-scenario stage. No content.md candidate, Claude review, Cursor review, human adjudication, benchmark label, or effectiveness claim exists yet.
