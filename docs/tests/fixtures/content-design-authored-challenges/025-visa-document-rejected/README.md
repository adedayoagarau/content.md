# Challenge 025: Travel document upload rejected

This original synthetic English-only scenario covers the `travel_mobility` domain, a `form_error` state, and a `inline` surface at `high` risk.

It is frozen at the authored-scenario stage. No content.md candidate, Claude review, Cursor review, human adjudication, benchmark label, or effectiveness claim exists yet.
