# Challenge 079: Data export includes shared records

This original synthetic English-only scenario covers the `privacy_permissions` domain, a `privacy_warning` state, and a `banner` surface at `high` risk.

It is frozen at the authored-scenario stage. No content.md candidate, Claude review, Cursor review, human adjudication, benchmark label, or effectiveness claim exists yet.
