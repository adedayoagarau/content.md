# Challenge 072: Automation paused after permission was revoked

This original synthetic English-only scenario covers the `ai_automation` domain, a `permission_loss` state, and a `status` surface at `high` risk.

It is frozen at the authored-scenario stage. No content.md candidate, Claude review, Cursor review, human adjudication, benchmark label, or effectiveness claim exists yet.
