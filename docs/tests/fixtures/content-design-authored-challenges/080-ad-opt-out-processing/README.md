# Challenge 080: Personalized ads opt-out processing

This original synthetic English-only scenario covers the `privacy_permissions` domain, a `pending_preference` state, and a `status` surface at `medium` risk.

It is frozen at the authored-scenario stage. No content.md candidate, Claude review, Cursor review, human adjudication, benchmark label, or effectiveness claim exists yet.
