# Challenge 096: Donation receipt needs correction

This original synthetic English-only scenario covers the `nonprofit_volunteering` domain, a `record_correction` state, and a `form` surface at `medium` risk.

It is frozen at the authored-scenario stage. No content.md candidate, Claude review, Cursor review, human adjudication, benchmark label, or effectiveness claim exists yet.
