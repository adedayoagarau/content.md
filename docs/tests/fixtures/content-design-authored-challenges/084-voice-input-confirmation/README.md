# Challenge 084: Voice input needs confirmation

This original synthetic English-only scenario covers the `accessibility_assistive` domain, a `ambiguous_input` state, and a `dialog` surface at `high` risk.

It is frozen at the authored-scenario stage. No content.md candidate, Claude review, Cursor review, human adjudication, benchmark label, or effectiveness claim exists yet.
