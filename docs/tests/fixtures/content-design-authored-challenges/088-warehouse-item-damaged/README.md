# Challenge 088: Warehouse pick item damaged

This original synthetic English-only scenario covers the `logistics_inventory` domain, a `exception` state, and a `status` surface at `medium` risk.

It is frozen at the authored-scenario stage. No content.md candidate, Claude review, Cursor review, human adjudication, benchmark label, or effectiveness claim exists yet.
