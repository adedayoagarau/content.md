# Challenge 093: Sports ticket transfer pending

This original synthetic English-only scenario covers the `events_bookings` domain, a `pending_recipient_action` state, and a `status` surface at `medium` risk.

It is frozen at the authored-scenario stage. No content.md candidate, Claude review, Cursor review, human adjudication, benchmark label, or effectiveness claim exists yet.
