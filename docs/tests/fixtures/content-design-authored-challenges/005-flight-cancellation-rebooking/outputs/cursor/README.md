# Cursor review output

Open `review-packet.json`, read only its referenced scenario, candidate, and
prompt, and save Cursor's exact response as `review.json`. Do not inspect
Claude's folder or response.
