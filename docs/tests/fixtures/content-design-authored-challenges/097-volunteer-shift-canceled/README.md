# Challenge 097: Volunteer shift canceled

This original synthetic English-only scenario covers the `nonprofit_volunteering` domain, a `schedule_change` state, and a `email` surface at `medium` risk.

It is frozen at the authored-scenario stage. No content.md candidate, Claude review, Cursor review, human adjudication, benchmark label, or effectiveness claim exists yet.
