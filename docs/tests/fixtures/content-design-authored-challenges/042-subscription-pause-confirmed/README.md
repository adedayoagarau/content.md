# Challenge 042: Subscription pause confirmation

This original synthetic English-only scenario covers the `media_subscriptions` domain, a `success_with_consequence` state, and a `confirmation` surface at `high` risk.

It is frozen at the authored-scenario stage. No content.md candidate, Claude review, Cursor review, human adjudication, benchmark label, or effectiveness claim exists yet.
