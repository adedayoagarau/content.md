# Challenge 041: Downloaded episode expired

This original synthetic English-only scenario covers the `media_subscriptions` domain, a `content_unavailable` state, and a `empty` surface at `low` risk.

It is frozen at the authored-scenario stage. No content.md candidate, Claude review, Cursor review, human adjudication, benchmark label, or effectiveness claim exists yet.
