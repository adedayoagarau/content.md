# Challenge 089: Order courier reassigned

This original synthetic English-only scenario covers the `logistics_inventory` domain, a `service_change` state, and a `push` surface at `low` risk.

It is frozen at the authored-scenario stage. No content.md candidate, Claude review, Cursor review, human adjudication, benchmark label, or effectiveness claim exists yet.
