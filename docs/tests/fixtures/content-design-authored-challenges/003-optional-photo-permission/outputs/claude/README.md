# Claude review output

Open `review-packet.json`, read only its referenced scenario, candidate, and
prompt, and save Claude's exact response as `review.json`. Do not inspect
Cursor's folder or response.
