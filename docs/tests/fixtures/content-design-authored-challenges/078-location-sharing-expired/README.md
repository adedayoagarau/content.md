# Challenge 078: Temporary location sharing expired

This original synthetic English-only scenario covers the `privacy_permissions` domain, a `expiry` state, and a `confirmation` surface at `medium` risk.

It is frozen at the authored-scenario stage. No content.md candidate, Claude review, Cursor review, human adjudication, benchmark label, or effectiveness claim exists yet.
