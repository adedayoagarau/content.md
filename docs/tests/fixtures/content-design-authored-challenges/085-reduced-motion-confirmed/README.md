# Challenge 085: Reduced motion preference confirmed

This original synthetic English-only scenario covers the `accessibility_assistive` domain, a `preference_change` state, and a `confirmation` surface at `low` risk.

It is frozen at the authored-scenario stage. No content.md candidate, Claude review, Cursor review, human adjudication, benchmark label, or effectiveness claim exists yet.
