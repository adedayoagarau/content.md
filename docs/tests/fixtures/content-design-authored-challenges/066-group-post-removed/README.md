# Challenge 066: Group post removed by a moderator

This original synthetic English-only scenario covers the `social_community` domain, a `moderation_action` state, and a `email` surface at `medium` risk.

It is frozen at the authored-scenario stage. No content.md candidate, Claude review, Cursor review, human adjudication, benchmark label, or effectiveness claim exists yet.
