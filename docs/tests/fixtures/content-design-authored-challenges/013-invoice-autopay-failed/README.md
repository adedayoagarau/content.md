# Challenge 013: Invoice autopay failed

This original synthetic English-only scenario covers the `payments_billing` domain, a `recoverable_error` state, and a `email` surface at `high` risk.

It is frozen at the authored-scenario stage. No content.md candidate, Claude review, Cursor review, human adjudication, benchmark label, or effectiveness claim exists yet.
