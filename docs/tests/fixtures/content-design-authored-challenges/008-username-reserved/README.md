# Challenge 008: Requested username is reserved

This original synthetic English-only scenario covers the `account_security` domain, a `form_error` state, and a `inline` surface at `medium` risk.

It is frozen at the authored-scenario stage. No content.md candidate, Claude review, Cursor review, human adjudication, benchmark label, or effectiveness claim exists yet.
