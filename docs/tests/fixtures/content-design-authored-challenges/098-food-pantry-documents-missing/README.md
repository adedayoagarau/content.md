# Challenge 098: Food pantry eligibility documents missing

This original synthetic English-only scenario covers the `nonprofit_volunteering` domain, a `blocked_dependency` state, and a `status` surface at `high` risk.

It is frozen at the authored-scenario stage. No content.md candidate, Claude review, Cursor review, human adjudication, benchmark label, or effectiveness claim exists yet.
