# Challenge 023: Hotel overbooking alternative

This original synthetic English-only scenario covers the `travel_mobility` domain, a `material_choice` state, and a `email` surface at `critical` risk.

It is frozen at the authored-scenario stage. No content.md candidate, Claude review, Cursor review, human adjudication, benchmark label, or effectiveness claim exists yet.
