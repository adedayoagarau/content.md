# Challenge 011: Possible duplicate card charge

This original synthetic English-only scenario covers the `payments_billing` domain, a `unknown_outcome` state, and a `status` surface at `critical` risk.

It is frozen at the authored-scenario stage. No content.md candidate, Claude review, Cursor review, human adjudication, benchmark label, or effectiveness claim exists yet.
