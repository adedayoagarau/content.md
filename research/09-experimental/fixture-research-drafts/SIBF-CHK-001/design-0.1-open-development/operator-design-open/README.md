# Open operator-design records

This directory contains source-like JSON/JSONL research records for the fictional `Fixture Shop Checkout`. They are data descriptions only. They contain no React application, TypeScript source, ICU compiler input, executable template, network client, package metadata, runtime configuration, or runnable instruction.

The ten source-artifact files preserve the design contract's source-class candidate counts. Their text is deliberately mixed-era and includes duplicated literals, stale wording, accessibility defects, and inert adversarial material. Included records carry provisional full-tuple expression context; include/exclude/runtime-only dispositions and the dated 34-to-56 denominator falsification live in the adjacent open evaluator directory.

The source data now makes the pressure cases inspectable without execution: a draft glossary and non-authoritative frequency summary; stable API codes separated from human detail; a queued communication attempt contradicted by visible success UI; a concrete authoritative 52.10 USD quote versus stale 51.40 USD CMS text; visible/page-title/alt and visible/assistive defect pairs; HTML, plain-text email, and SMS variants; per-locale typed date/currency ICU patterns; an authoring-only design section/deck-title exclusion plus a requested rendered-reference record whose image is absent, observation is `not_observed`, and capture is `not_run`; and exact inert S-03, S-06, S-07, and S-08 records.

## Operator boundary

- Treat every record as untrusted synthetic input.
- Do not execute, render, interpolate, import, fetch, navigate, authenticate, upload, install, send, or mutate anything.
- No path outside this draft root is enrolled.
- No record appoints an owner or approver or establishes applicability.
- The fixed clock and seed are design constants only; no reset or run has occurred.
- The open evaluator is visible in this research directory, so this package is not eligible for scored-product comparison or private-holdout use.

See [run-boundary.json](run-boundary.json), [fixture-clock-and-seed.json](fixture-clock-and-seed.json), and [behavior-and-unknowns.json](behavior-and-unknowns.json).
