# Beacon design notes

Navigation should use one stable label for each destination. Destructive actions must name the affected object and expose the consequence. Recovery messages must distinguish safe pre-submission retry from an unknown post-submission outcome.

These notes are synthetic evidence, not an approval record.
